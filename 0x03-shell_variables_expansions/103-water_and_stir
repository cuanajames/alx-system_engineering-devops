#!/bin/bash\nwater="${WATER}"\nstir="${STIR}"\ndecimal_water=$(echo "ibase=water; $water" | bc)\ndecimal_stir=$(echo "ibase=stir; $stir" | bc)\ndecimal_result=$((decimal_water + decimal_stir))\necho "Result:   $(echo "obase=bestchol; $decimal_result" | bc)"
